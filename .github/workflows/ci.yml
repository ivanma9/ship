name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  build-and-check:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: ship_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build shared
        run: pnpm build:shared

      - name: Type check
        run: pnpm type-check

      - name: Check type violation ceiling (legacy)
        run: ./scripts/count-type-violations.sh 416

      - name: Check type violation ceiling (ratchet)
        run: node scripts/check-type-ceiling.mjs

      - name: Run database migrations
        run: pnpm db:migrate
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/ship_test

      - name: Run unit tests
        run: pnpm test
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/ship_test

      - name: Build web (analyze)
        run: pnpm --filter web run build:analyze
        env:
          VITE_API_URL: ''

      - name: Check bundle budget
        run: pnpm --filter web run bundle:check

      - name: Upload bundle analysis
        uses: actions/upload-artifact@v4
        with:
          name: bundle-analysis
          path: |
            web/dist/bundle-report.html
            web/dist/bundle-size.json
          if-no-files-found: error

  accessibility-gates:
    # Zero-tolerance: fails if any serious/critical axe violation exists on covered routes.
    # Activate only after all fixes in 007-a11y-completion-gates are merged.
    #
    # No postgres service block needed: the isolated-env fixture (e2e/fixtures/isolated-env.ts)
    # uses testcontainers to spin up its own per-worker PostgreSqlContainer, applies migrations
    # directly via runMigrations(), and seeds test data — entirely independent of any YAML service.
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build shared
        run: pnpm build:shared

      - name: Install Playwright browsers
        run: pnpm exec playwright install --with-deps chromium

      - name: Build API
        run: pnpm build:api

      - name: Build web for preview
        # VITE_API_URL is intentionally left blank here: the isolated-env fixture sets API_PORT
        # at runtime so vite preview proxies to the per-worker API server on a dynamic port.
        # A build-time VITE_API_URL would be overridden by the fixture's proxy anyway.
        run: pnpm --filter @ship/web build

      - name: Run accessibility gate tests
        run: pnpm exec playwright test e2e/accessibility.spec.ts --reporter=list
        env:
          PLAYWRIGHT_WORKERS: 1

      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: a11y-gate-results
          path: test-results/
          retention-days: 30
